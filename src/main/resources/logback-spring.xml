<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Spring Boot 기본 로그 설정 불러오기 -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- 로그 패턴 및 디렉토리 설정 -->
    <property name="LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} [%thread] %highlight(%-5level) %clr(%logger{36}){cyan} - %msg%n%xEx"/>
    <!-- LOG_DIR는 application.yml에서 logging.file.path로 외부 설정할 수 있습니다 -->
    <property name="LOG_DIR" value="logs"/>

    <!-- 콘솔 출력: 색상 적용 -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- 일반 로그 파일 (INFO 이하 모두 기록) -->
    <appender name="File" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/app.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <!-- 에러 이상 로그는 제외 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>DENY</onMatch>
            <onMismatch>NEUTRAL</onMismatch>
        </filter>
    </appender>

    <!-- 에러 로그 파일 (ERROR 이상만 기록) -->
    <appender name="ErrorFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>7</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <!-- ERROR 레벨 이상만 기록 -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>

    <!-- 공통 설정: 모든 환경에서 공통적으로 적용 -->
    <logger name="org.hibernate.SQL" level="INFO"/>
    <logger name="com.lineinc" level="DEBUG"/>
    
    <!-- Broken Pipe 및 클라이언트 연결 해제 에러는 WARN 레벨로 처리 -->
    <logger name="org.springframework.web.context.request.async" level="WARN"/>
    <logger name="org.springframework.web.servlet.mvc.method.annotation" level="WARN"/>
    <logger name="org.apache.tomcat" level="WARN"/>
    <logger name="org.apache.coyote" level="WARN"/>
    
    <!-- 예외 처리 관련 로그 레벨 조정 -->
    <logger name="com.lineinc.erp.api.server.shared.exception" level="WARN"/>

    <!-- 비동기 로깅으로 성능 향상 -->
    <appender name="AsyncFile" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="File"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <appender name="AsyncErrorFile" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ErrorFile"/>
        <queueSize>256</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- 기본 환경 fallback -->
    <root level="INFO">
        <appender-ref ref="Console"/>
        <appender-ref ref="AsyncFile"/>
        <appender-ref ref="AsyncErrorFile"/>
    </root>

    <!-- local, dev 환경 공통 설정 -->
    <springProfile name="local,dev">
        <logger name="org.hibernate.orm.jdbc.bind" level="TRACE"/>

        <root level="INFO">
            <appender-ref ref="Console"/>
            <appender-ref ref="AsyncFile"/>
            <appender-ref ref="AsyncErrorFile"/>
        </root>
    </springProfile>

    <!-- prod 환경: 콘솔 제외, 파일 로그만 저장 -->
    <springProfile name="prod">
        <logger name="org.hibernate.orm.jdbc.bind" level="OFF"/>

        <root level="INFO">
            <appender-ref ref="AsyncFile"/>
            <appender-ref ref="AsyncErrorFile"/>
        </root>
    </springProfile>

</configuration>