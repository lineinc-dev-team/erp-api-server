# JVM 옵션 설정 (Gradle 데몬에 적용)
org.gradle.jvmargs=-XX:MaxMetaspaceSize=384m -Xms256m -Xmx512m -Dfile.encoding=UTF-8

# 병렬 빌드 활성화 (원하면)
org.gradle.parallel=true

# 빌드 캐시 사용 (settings.gradle에서도 설정했지만 명시적으로 추가 가능)
org.gradle.caching=true